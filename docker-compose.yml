services:
  app:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8081:8000"
    env_file:
      - .env
    command: >
      bash -c "python manage.py migrate &&
               python manage.py runserver 0.0.0.0:8000"